{
    "app": {
        "addr": "localhost:8000",
        "read_timeout": "5s",
        "write_timeout": "5s",
        "idle_timeout": "5s"
    },
    "core": {
        "graceful_timeout": "10s"
    }
}